{
    "tasks": [
        {
            "label": "C/C++: cpp build active file",
            "type": "shell",
            // if you want to run locally without using docker container, please change LLVM_DIR & SVF_DIR 
            "command": "cmake -g -DCMAKE_BUILD_TYPE=Debug -DZ3_DIR=/Users/charles/z3.obj . && make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}